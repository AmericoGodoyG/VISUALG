Algoritmo "tabuada"

Var

 n, i, t : inteiro

Inicio

 t <- 0
 // seleção da tabuada na var n
 escreva("Insira a tabuada: ")
 leia(n)

// contador
 para i de 1 ate 10 faca
    t <- n * i // multi. tabuada * contador / atribuição do resultado a var t.
    escreval(n," x ",i," =",t) // saída de dados
 fimpara

Fimalgoritmo